import React, { ReactElement } from 'react';

import { MainLayout } from '../../layout';

function ReactJs(): ReactElement {
  return <div>ReactJs Tag</div>;
}
ReactJs.Layout = MainLayout;
export default ReactJs;
